en:
  js:
    pretty:
      editor:
        input: Input
        output: Output
        indent_num:
          label: Indent
          placeholder: Number of indentation spaces
        form_submit:
          label: Submit
          disable: Processing

    json_xml:
      editor:
        input: Input JSON
        output: Output XML
        settings: Settings
        mode:
          label: Output mode
          pretty: Pretty printed
          one_line: Oneliner
        form_submit:
          label: Convert
          disable: Processing

    schema:
      editor:
        input_json: JSON
        input_schema: Schema (Draft 4)
        output: Output
        form_submit:
          label: Validate
          disable: Processing

  